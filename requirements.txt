# Core dependencies
PyYAML>=6.0

# PostgreSQL connector
psycopg2-binary>=2.9.0

# Elasticsearch connector
elasticsearch>=8.0.0

# Gmail connector
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# Airflow (for connection management)
apache-airflow>=2.5.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0

pyyaml
numpy
transformers
torch
torch --index-url https://download.pytorch.org/whl/cpu # for cpu only
soundfile
elasticsearch
faiss-cpu
msgpack
psycopg2-binary
asyncpg
google_auth_oauthlib
google-api-python-client
bs4
pandas
nltk
docling-core --no-cache-dir --no-deps # for cpy only

# apache
apache-airflow
apache-airflow-providers-postgres

export AIRFLOW_VERSION=2.10.2
export PYTHON_VERSION="3.10"
export CONSTRAINT_URL="https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt"
"apache-airflow==${AIRFLOW_VERSION}" --constraint "${CONSTRAINT_URL}"